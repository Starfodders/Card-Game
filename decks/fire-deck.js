const base = require('./base-deck')
const {Deck, Card} = base;                  //extracts the classes from base
// console.log(Card);

const firePunch = new Card ('fire punch','fire', 1)
console.log(firePunch);